<?xml version="1.0" encoding="UTF-8"?>
<sitemapindex xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <!-- Static pages sitemap -->
  <sitemap>
    <loc>https://autora.be/sitemap-pages.xml</loc>
    <lastmod>2026-05-30</lastmod>
  </sitemap>
  <!-- NOTE: Dynamic vehicle sitemaps (sitemap-vehicles-*.xml) are temporarily
       disabled. The Cloudflare Pages `_redirects` proxy to the Supabase edge
       function `dynamic-sitemap` returns 404 (SPA catch-all takes precedence
       over external redirects). To re-enable, either:
         (a) deploy a dedicated Cloudflare Worker that proxies these paths, or
         (b) generate the vehicle sitemaps statically at build time.
       Until then, vehicle listings will be discovered organically by Googlebot
       via internal links from the homepage and /recherche. -->
</sitemapindex>
