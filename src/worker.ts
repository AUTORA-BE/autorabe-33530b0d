/**
 * ⚠️ DEPRECATED — not deployed.
 *
 * Production hosting has moved to Netlify. The dynamic Open Graph / SSR
 * meta logic now lives in `netlify/edge-functions/og-meta.ts`.
 *
 * This file is intentionally empty and kept only to avoid breaking any
 * external reference. It is NOT loaded by Lovable Hosting (no Cloudflare
 * Worker is ever executed on autora.be), and it is NOT used by Netlify.
 *
 * Safe to delete.
 */
export {};
